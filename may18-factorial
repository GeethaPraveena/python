#write a program to print factorial of the given number without using looping statements,
def fact(n):
    if n==0:
        return 1
    else:
        return n*fact(n-1)
n=int(input())
print(fact(n))
